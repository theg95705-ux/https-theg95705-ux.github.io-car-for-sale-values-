@import url("https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap");

:root {
  --primary: #4f8dff;
  --primary2: #70a8ff;

  --background: #06101f;
  --background2: #0a1930;

  --text: #ffffff;
  --muted: #b9c9e9;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Inter", sans-serif;
}

html {
  scroll-behavior: smooth;
}

body {
  background: radial-gradient(
    circle at top,
    #194b97 0%,
    #0d2448 30%,
    #071321 100%
  );

  color: white;

  overflow-x: hidden;

  min-height: 100vh;
}

/* Blue Glow */

body::before {
  content: "";

  position: fixed;

  top: -400px;
  left: 50%;

  transform: translateX(-50%);

  width: 900px;
  height: 900px;

  background: radial-gradient(
    circle,
    rgba(79, 141, 255, 0.45),
    transparent 70%
  );

  filter: blur(60px);

  z-index: -1;

  pointer-events: none;
}

/* ========================= */
/* NAVBAR */
/* ========================= */

.navbar {
  position: fixed;

  top: 0;
  left: 0;

  width: 100%;
  height: 82px;

  display: flex;

  align-items: center;
  justify-content: space-between;

  padding: 0 70px;

  background: rgba(7, 18, 34, 0.7);

  backdrop-filter: blur(18px);

  border-top: 3px solid var(--primary);

  border-bottom: 1px solid rgba(255, 255, 255, 0.05);

  z-index: 999;
}

.logo {
  font-size: 30px;

  font-weight: 800;

  letter-spacing: 0.3px;
}

.logo span {
  color: var(--primary);
}

nav {
  display: flex;

  gap: 40px;
}

nav a {
  text-decoration: none;

  color: white;

  font-weight: 600;

  transition: 0.3s;
}

nav a:hover {
  color: var(--primary);
}

/* Buttons */

.button,
.primary,
.secondary {
  display: inline-flex;

  align-items: center;

  justify-content: center;

  padding: 15px 34px;

  border-radius: 14px;

  font-weight: 700;

  text-decoration: none;

  transition: 0.35s;
}

.button,
.primary {
  background: linear-gradient(180deg, #6ca8ff, #4f8dff);

  box-shadow: 0 0 30px rgba(79, 141, 255, 0.45);

  color: white;
}

.button:hover,
.primary:hover {
  transform: translateY(-3px);

  box-shadow: 0 0 45px rgba(79, 141, 255, 0.75);
}

.secondary {
  margin-left: 18px;

  background: rgba(255, 255, 255, 0.05);

  border: 1px solid rgba(255, 255, 255, 0.08);

  color: white;

  backdrop-filter: blur(10px);
}

.secondary:hover {
  background: rgba(79, 141, 255, 0.2);
}

/* ========================= */
/* HERO */
/* ========================= */

.hero {
  position: relative;

  display: flex;

  justify-content: center;

  align-items: center;

  min-height: 100vh;

  padding-top: 90px;

  overflow: hidden;
}

.content {
  position: relative;

  z-index: 20;

  max-width: 900px;

  text-align: center;

  padding: 20px;
}

.content h1 {
  font-size: 84px;

  font-weight: 900;

  line-height: 1;

  margin-bottom: 30px;

  text-shadow: 0 0 40px rgba(79, 141, 255, 0.45);
}

.content span {
  color: var(--primary);
}

.content p {
  max-width: 760px;

  margin: auto;

  font-size: 22px;

  line-height: 1.8;

  color: var(--muted);

  margin-bottom: 45px;
}

.buttons {
  display: flex;

  justify-content: center;

  gap: 18px;

  flex-wrap: wrap;
}
/* ========================= */
/* HERO BACKGROUND SLIDER */
/* ========================= */

.background {
  position: absolute;

  inset: 0;

  overflow: hidden;

  z-index: 1;
}

.background img {
  position: absolute;

  inset: 0;

  width: 100%;
  height: 100%;

  object-fit: cover;

  opacity: 0;

  animation: heroSlider 18s infinite;

  transform: scale(1.12);

  filter: brightness(0.65);
}

/* Image Timing */

.background img:nth-child(1) {
  animation-delay: 0s;
}

.background img:nth-child(2) {
  animation-delay: 6s;
}

.background img:nth-child(3) {
  animation-delay: 12s;
}

/* Fade Animation */

@keyframes heroSlider {
  0% {
    opacity: 0;

    transform: scale(1.12);
  }

  6% {
    opacity: 1;
  }

  30% {
    opacity: 1;

    transform: scale(1);
  }

  36% {
    opacity: 0;
  }

  100% {
    opacity: 0;

    transform: scale(1);
  }
}

/* ========================= */
/* DARK OVERLAY */
/* ========================= */

.overlay {
  position: absolute;

  inset: 0;

  background:
    linear-gradient(rgba(5, 15, 30, 0.55), rgba(5, 15, 30, 0.9)),
    radial-gradient(circle at top, rgba(79, 141, 255, 0.2), transparent 65%);

  z-index: 2;
}

/* ========================= */
/* HERO GLOW */
/* ========================= */

.hero-glow {
  position: absolute;

  top: 50%;

  left: 50%;

  transform: translate(-50%, -50%);

  width: 900px;

  height: 900px;

  background: radial-gradient(
    circle,
    rgba(79, 141, 255, 0.18),
    transparent 70%
  );

  filter: blur(90px);

  pointer-events: none;

  z-index: 3;
}

/* ========================= */
/* FEATURE CARDS */
/* ========================= */

.features {
  display: grid;

  grid-template-columns: repeat(3, 1fr);

  gap: 30px;

  max-width: 1200px;

  margin: 120px auto;

  padding: 0 40px;
}

.card {
  background: rgba(15, 32, 60, 0.55);

  backdrop-filter: blur(18px);

  border: 1px solid rgba(79, 141, 255, 0.18);

  border-radius: 22px;

  padding: 35px;

  transition: 0.35s;

  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.25);
}

.card:hover {
  transform: translateY(-10px);

  border-color: #4f8dff;

  box-shadow: 0 25px 60px rgba(79, 141, 255, 0.2);
}

.card h2 {
  margin-bottom: 18px;

  font-size: 28px;

  color: #6ea8ff;
}

.card p {
  color: #b9c9e9;

  line-height: 1.7;
}
